// Socket.IO retired for chat (see DashboardBinding) — this was never
// referenced anywhere else in the app, so it's just commented out.
// import 'package:getgabs/domain/controllers/sockets/sockets_controller.dart';
// SocketsController socketContoller = SocketsController.instance;

